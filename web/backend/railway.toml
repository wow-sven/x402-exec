providers = ["node"]

[phases.setup]
nixPkgs = ['nodejs']
cmds = [
  "corepack enable",
  "corepack prepare pnpm@10.7.0 --activate",
  "pnpm --version"
]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm run build"
]

[start]
cmd = "pnpm run start"

