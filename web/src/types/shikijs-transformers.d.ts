// TypeScript stub so tsc doesn't fail when '@shikijs/transformers' isn't installed.
// We don't use the server-side code block in this app build; client variant handles highlighting.
declare module '@shikijs/transformers';

